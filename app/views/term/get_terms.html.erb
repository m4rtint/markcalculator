<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
      <div class="navbar-header">
         <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
           <span class="sr-only">Toggle navigation</span>
           <span class="icon-bar"></span>
           <span class="icon-bar"></span>
           <span class="icon-bar"></span>
         </button>
      </div>

              <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
              <% @terms.each do |t| %>
            <li class= <%= 'active' if params[:tid] == t.id.to_s%>>
                <%= link_to t.name, "/term/"+t.id.to_s %>
            </li>
            <% end %>
          </ul>

    <%= form_tag({:controller => "term", :action => "post_terms"}, {:class => "navbar-form navbar-left", :role => "search"}) do %>

        <div class="form-group">
            <%= text_field_tag :termName, "" , class:'form-control', :placeholder => "Term Name / Year", :required => true%>
        </div>
        <%= submit_tag "+", :class => "btn btn-success" %>

    <% end %>

    <?--- need to fix this shit, need some sort of confirmation to delete --->
    <%= form_tag delete_term_path(:tid => params[:tid]), :class => "navbar-form navbar-right", :role => "search", method: :delete do %>
        <%= submit_tag "X",  :class => "btn btn-danger"%>
    <% end %>
  </div>
</nav>

<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
      <div class="navbar-header">
         <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
           <span class="sr-only">Toggle navigation</span>
           <span class="icon-bar"></span>
           <span class="icon-bar"></span>
           <span class="icon-bar"></span>
         </button>
      </div>

              <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
              <% @terms.each do |t| %>
            <li class= <%= 'active' if params[:tid] == t.id.to_s%>>
                <%= link_to t.name, "/term/"+t.id.to_s %>
            </li>
            <% end %>
          </ul>

    <%= form_tag({:controller => "term", :action => "post_terms"}, {:class => "navbar-form navbar-left", :role => "search"}) do %>

        <div class="form-group">
            <%= text_field_tag :termName, "" , class:'form-control', :placeholder => "Add Term", :required => true%>
        </div>
        <%= button_tag(type: 'submit', class: "btn btn-success") do %>
            <span class="glyphicon glyphicon-plus"></span>
        <% end %>

    <% end %>

    <?--- TODO - need to fix this shit, need some sort of confirmation to delete --->
    <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> <%= current_user.name %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
                <li><%= link_to "Delete Terms", '/settings' %></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to "Sign Out", '/sign_out', method: :delete %></li>
            </ul>
        </li>
    </ul>
  </div>
</nav>
